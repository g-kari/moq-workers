{
  "name": "moq-workers-frontend",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:broadcast": "playwright test --config=playwright.prod.config.ts"
  },
  "dependencies": {
    "@moq/publish": "^0.2.3",
    "@moq/watch": "^0.2.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "typescript": "^5.8.3",
    "vite": "^6.2.6"
  }
}
